# .gitattributes
* text=auto

# Denote all files that are truly binary and should not be modified.
*.png binary
*.svg binary

# Remove trailing whitespace from these files when they are added
*.py   text diff=python
*.js   text
*.ts   text
*.html text
*.scss text

# Explicitly declare text files we want to always be normalized and converted
# to native line endings on checkout.
*.py    text
*.js    text
*.ts    text
*.html  text
*.scss  text